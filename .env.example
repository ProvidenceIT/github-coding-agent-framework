# =============================================================================
# Claude Agent SDK Authentication
# =============================================================================
# You can use either OAuth tokens (Claude Max subscription) or API keys.
# The token rotator will automatically detect all tokens and rotate between
# them when rate limits are hit.

# -----------------------------------------------------------------------------
# Option 1: Claude Max Subscription (OAuth Tokens)
# -----------------------------------------------------------------------------
# Get this by running: claude setup-token
#
# For 24/7 operation with Opus 4.5, you'll need multiple accounts:
# - Max 5x ($100/mo): ~15-35 hours Opus/week -> need 5-12 accounts for 24/7
# - Max 20x ($200/mo): ~24-40 hours Opus/week -> need 5-7 accounts for 24/7

# Primary OAuth token (used first)
CLAUDE_CODE_OAUTH_TOKEN=your-oauth-token-here

# Backup OAuth tokens (rotated to when limits hit)
# CLAUDE_CODE_OAUTH_TOKEN_1=backup-oauth-token-1
# CLAUDE_CODE_OAUTH_TOKEN_2=backup-oauth-token-2
# CLAUDE_CODE_OAUTH_TOKEN_3=backup-oauth-token-3
# CLAUDE_CODE_OAUTH_TOKEN_BACKUP=another-backup-token

# -----------------------------------------------------------------------------
# Option 2: API Keys (Pay-as-you-go)
# -----------------------------------------------------------------------------
# Get API keys from: https://console.anthropic.com/
# Pricing for Opus 4.5:
# - Input: $15 / million tokens
# - Output: $75 / million tokens

# Primary API key (used after OAuth tokens if both are set)
# ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Backup API keys
# ANTHROPIC_API_KEY_1=sk-ant-backup-key-1
# ANTHROPIC_API_KEY_2=sk-ant-backup-key-2
# ANTHROPIC_API_KEY_BACKUP=sk-ant-another-backup

# -----------------------------------------------------------------------------
# Token Rotation Settings
# -----------------------------------------------------------------------------
# Cooldown period (minutes) before a rate-limited token can be reused
# TOKEN_COOLDOWN_MINUTES=5

# -----------------------------------------------------------------------------
# GitHub Authentication
# -----------------------------------------------------------------------------
# Note: GitHub authentication is handled by the gh CLI
# Run 'gh auth login' to authenticate with GitHub
# No API key needed - gh manages OAuth automatically

# Optional: GitHub token for API access (if not using gh CLI)
# GITHUB_TOKEN=ghp_your-github-token

# -----------------------------------------------------------------------------
# Agent Configuration
# -----------------------------------------------------------------------------
# Default model (claude-opus-4-5-20251101, claude-sonnet-4-20250514)
# CLAUDE_MODEL=claude-opus-4-5-20251101

# Default GitHub organization for new repos
# GITHUB_ORG=YourOrgName
